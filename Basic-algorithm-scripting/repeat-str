function repeatStringNumTimes(str, num) {
  //return str.repeat(num);
  let newStr= ""
  let i=0
  while(i < num){
    newStr += str;
    i++
  }
  return newStr;
}

console.log(repeatStringNumTimes("abc", 3));